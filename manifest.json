install.packages("rsconnect")
setwd("C:\Users\PERRYM\Documents\ekspoc2")  # make sure this is the dir with app.R
rsconnect::writeManifest(
  appFiles      = c("app.R"),   # include additional files if your app needs them
  appPrimaryDoc = "app.R",
  appMode       = "shiny"
)